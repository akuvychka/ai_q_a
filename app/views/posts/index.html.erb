<div class="top tar">
  <%= link_to(I18n.t('posts.new'), new_post_path) %>
</div>

<div class="list">
  <dl>
    <% @posts.each do |post| %>
      <div class="list-item">
        <li class="left tal">
          <%= link_to(post.header, post_path(post)) %>
        </li>
      </div>
    <% end %>
  </dl>
</div>
